[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "know-do-graph"
dynamic = ["version"]
description = "A wiki-native, agent-oriented infrastructure for executable knowledge, operational memory, and capability discovery."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "networkx>=3.0",
    "fastapi>=0.100",
    "uvicorn[standard]",
    "typer>=0.24.1",
    "sqlalchemy>=2.0",
    "rich>=13.0",
    "pyyaml>=6.0",
    "openai>=1.0",
    "httpx>=0.25",
    "duckduckgo-search>=6.0",
    "python-dotenv>=1.0",
    "prompt_toolkit>=3.0",
    "sqlite-vec>=0.1.6",
    "numpy>=1.24",
]

[project.optional-dependencies]
embeddings = ["sentence-transformers>=2.7"]
local-embeddings = ["sentence-transformers>=2.7"]
dev = [
    "build>=1.2",
    "mypy>=1.10",
    "pytest>=8.0",
    "ruff>=0.5",
]

[project.scripts]
know-do-graph = "know_do_graph.cli:app"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.vcs]
version-file = "core/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["agents", "api", "core", "examples", "know_do_graph"]

[tool.hatch.build.targets.wheel.force-include]
"main.py" = "main.py"
"assets/starter.db" = "core/resources/starter.db"
"frontend/dist" = "frontend/dist"

[tool.hatch.build.targets.sdist.force-include]
"assets/starter.db" = "assets/starter.db"
"frontend/dist" = "frontend/dist"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
follow_imports = "skip"
files = [
    "core/extraction",
    "core/retrieval/fusion.py",
    "core/retrieval/vector_store.py",
    "core/schemas/edge.py",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
